name: newsapi_clean_architecture_cubit
description: implement clean architecture with bloc/cubit state management.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  connectivity: ^3.0.6
  cupertino_icons: ^1.0.2
  dartz: ^0.10.0
  dio: ^4.0.0
  equatable: ^2.0.3
  flutter:
    sdk: flutter
  flutter_bloc: ^7.3.0
  flutter_dotenv: ^5.0.2
  get: ^4.3.8
  json_annotation: ^4.1.0
  kiwi: ^3.0.0
  lint: ^1.7.2
  meta: ^1.7.0
  path: ^1.8.0
  path_provider: ^2.0.5
  sembast: ^3.1.1

dev_dependencies:
  build_runner: ^2.0.4
  flutter_lints: ^1.0.0
  flutter_test:
    sdk: flutter
  json_serializable: ^4.1.3
  kiwi_generator: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    - .env
